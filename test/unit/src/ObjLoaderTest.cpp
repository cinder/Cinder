
#include "catch.hpp"
#include "cinder/ObjLoader.h"

using namespace cinder;

TEST_CASE("ObjLoader")
{
	SECTION("ObjLoader can parse simple files")
	{
		// TODO: load a file and check some basic facts about it (e.g. unit size, color)
	}

	SECTION("ObjLoader successfully parses material files")
	{

	}

	SECTION("ObjLoader handles files with line continuations")
	{

	}
}
